<div id="character-details-react-app">
    <% files.css.forEach(function (src) { %>
        <link rel="stylesheet" href="<%= src %>">
    <% }) %>
    <div id="character-details-react-app-root"></div>
    <% files.js.forEach(function (src) { %>
            <script src="<%= src %>"></script>
    <% }) %>
</div>
